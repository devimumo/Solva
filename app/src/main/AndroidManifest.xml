<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.solva">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/new_icons_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/new_icons_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Solva">
        <activity android:name=".SpareParts.Check_out.Checkout"></activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Vendor.AddGarage"
            android:label="@string/title_activity_add_garage" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Repair_Car.Repair_Car"
            android:label="@string/title_activity_repair__car" />
        <activity android:name=".SpareParts.View_Items_In_Cart.Items_in_cart" />
        <activity android:name=".SpareParts.Add_to_cart.Add_To_cart" />
        <activity android:name=".SpareParts.Spares_Search_Dashboard" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_main_acivity" />
    </application>

</manifest>