package com.example.solva.View_model_items

import android.app.Application
import com.example.solva.Room_Database.App_database.Items_added_to_cart_DB

class SparesApplication: Application() {

   // val database by lazy { Items_added_to_cart_DB.getDtabase(this)}
   ///val repository by lazy { Spares_Repository(database.) }
}